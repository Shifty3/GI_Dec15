****The evnt loop

the call stack keeps track of all the code
-it mian i sthe first functio to get added becuas enode wrapped our code
-the call stack top runs befroe the bottom

an event call back kpair is when a fucntion is being ran by api and then return back to the call stackß

node api alloows asyc fucntions to runs

encodeURI allows special chars to be rran in the url so the search dont crash

destructuring allows you to take multiple properties forom a object and assign them to vars in a more readable way

cant send two responses in a html request

hbs does not update, you hvae to exit node and re-run it to see changes